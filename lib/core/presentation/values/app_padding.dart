part of 'values.dart';

abstract class AppPadding {
  static const defaultScreenPadding = EdgeInsets.all(16.0);

  static const defaultPadding = EdgeInsets.all(16.0);

  static const defaultButtonPadding = EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0);
}
